[user]
	name = {{@@ username @@}}
	email = {{@@ username @@}}@{{@@ mail @@}}

[init]
    defaultBranch = main

[pull]
	rebase = false

[pager]
    # diff-highlight is script provided by git that shows word-by-word diff
    log  = diffr | less
    show = diffr | less
    diff = diffr | less
